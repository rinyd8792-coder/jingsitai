# 静思台 V0.2.2 改造说明

本轮目标：让静思台从“能管理已有任务”升级为“能接住现实中新出现的事情”。

## 已新增

### 1. 全局新建任务入口
- 顶部常驻「新建任务」按钮
- 支持标题、描述、分类、所属项目、截止时间、交付对象、交付物、来源网页 URL
- 直接复用现有 `createTask()` 数据层

### 2. Inbox → Task
- 拾思列表增加「转为任务」操作
- 自动把拾思内容带入任务标题/描述
- 如果备注中包含网页 URL，会自动带入来源网页字段
- 创建成功后自动把原 Inbox 项标记为已整理

### 3. 网页拾思浏览器插件（MVP）
目录：`extension/`

第一版不直接跨域写 localStorage，而是：
1. 插件读取当前网页标题与 URL
2. 打开静思台 `#/inbox?capture=1&title=...&url=...`
3. Inbox 自动将网页收入拾思

部署地址默认：
`https://rinyd8792-coder.github.io/jingsitai/#/inbox`

Chrome 安装方法：
- 打开 `chrome://extensions/`
- 开启「开发者模式」
- 选择「加载已解压的扩展程序」
- 选择本项目的 `extension` 文件夹

### 4. AI 配置入口
新增：`设置 → AI`

支持配置：
- OpenAI
- Anthropic
- Gemini
- 自定义 OpenAI-compatible API
- Base URL
- API Key
- Model

当前在线连通性测试优先支持 OpenAI-compatible `/models` 接口。

> 注意：当前 API Key 保存在 localStorage，仅适合个人测试。正式部署应改为服务端密钥管理 + 后端代理。

## 下一轮建议

V0.2.3：
- AI 任务拆节点
- AI 节点复盘（Scratchpad → 结论 / 遗留问题 / Next Action）
- Inbox AI 分类建议
- 任务创建后可立即进入“拆节点”流程
- 为云端同步引入 Supabase/PostgreSQL 或等价后端

# V0.2.5.2 火山连接修复

## 根因
GitHub Pages 浏览器直接请求火山方舟会出现 `Failed to fetch` / CORS 风险，同时 API Key 暴露在 localStorage。

## 修复架构
GitHub Pages → Jingsitai AI Proxy → 火山方舟 Coding Plan → glm-5.2

## 固定上游配置
- Base URL: `https://ark.cn-beijing.volces.com/api/coding/v3`
- Model: `glm-5.2`

## Worker 部署
进入 `worker` 目录：

```bash
npm install
npx wrangler login
npx wrangler secret put VOLC_API_KEY
npm run deploy
```

把部署后得到的 workers.dev 地址填进静思台的 AI Proxy URL。

## 说明
- 火山 API Key 只放 Worker secret，不再放浏览器。
- Worker 只允许 `https://rinyd8792-coder.github.io` Origin。
- `/health` 可用于检查 Worker 是否已配置。

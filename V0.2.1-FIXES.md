# 静思台 V0.2.1 修复说明

本修复版优先稳定“单线程执行 → 节点复盘 → 待续/下一步”的基础闭环。

## 已修复

1. Scratchpad 新增记录后不再清空当前节点的 Action 与 Deliverable。
2. “部分完成”不再自动跳到下一节点：保留当前节点，并按继续时间重新排期。
3. Waiting / Paused 恢复时同步恢复当前节点为 pending，可重新进入 Focus。
4. Today 与节点日期筛选统一使用本地日期，避免 UTC 跨日导致节点显示到错误日期。
5. NOW Focus 改为浏览器内每秒更新 UI，每 30 秒（以及暂停/切换时）持久化一次，减少 localStorage 高频写入。
6. “放弃”节点会继续推进到后续节点；“已完成”保持原有推进逻辑。

## 暂未包含

- 30 分钟起身提醒
- 自动动态重排后续所有节点
- AI 自动拆解节点
- 云端同步
